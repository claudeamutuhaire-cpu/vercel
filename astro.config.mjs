// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://vercel-git-main-claudeamutuhaire-cpu.vercel.app',  // ← YOUR REAL PREVIEW URL HERE (update after deploy if needed)
  // ... rest of your config (output: 'static', vite tweaks, etc.)
});
